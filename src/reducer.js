export default function (state = {}, action) {
    /* if (action.type == "RECIEVE FRIEND WANNABES") {
        state = Object.assign({}, state, {
            users: action.users,
        });
    }  */
    return state;
}
